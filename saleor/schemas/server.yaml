%YAML 1.1
---
"$schema": "http://json-schema.org/draft-06/schema#"
title: saleor daemon role
description: |
  Saleor daemon, server role
type: object
additionalProperties: false

required:
  - enabled

properties:
  enabled:
    description: |
      Enables saleor daemon service
    type: boolean
